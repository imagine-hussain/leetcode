#include <vector>

class Solution {
public:
    int numIslands(vector<vector<char>>& grid) {
        // do dfs and mark neighbour cells as visited if they are islands
        int island_count = 0;
        // Get size of grid
        int m = grid.len();
    }
};
