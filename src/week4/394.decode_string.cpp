#include <iostream>

using namespace std;

class Solution {
public:
    string decodeString(string s) {

        return do_decode(s, 0, s.length());
    }

    string do_decode(string& s, int lo, int hi) {
        int i = lo;
        int j = lo;

        string res;
        while (1) {
            // extract number
        }

        return "";
    }

};
